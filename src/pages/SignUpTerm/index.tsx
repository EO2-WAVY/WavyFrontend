import styled from "styled-components";
import { motion } from "framer-motion";

import Layout from "components/Common/Layout";

const SignUpTerm = () => {
    return (
        <Layout>
            <div>안녕하세요 나는 가입 절차입니다.</div>
        </Layout>
    );
};

export default SignUpTerm;

